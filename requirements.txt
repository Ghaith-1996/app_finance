# Python news ingestion worker dependencies
# Install with: pip install -r requirements.txt

edgartools>=2.7.0       # SEC EDGAR filing access
newsapi-python>=0.2.7   # NewsAPI global headlines
gnews>=0.4.2            # Google News RSS wrapper (no API key)
newspaper4k>=0.9.0      # Full article text extraction from URLs (replaces newspaper3k)
supabase>=2.10.0        # Supabase Python client
python-dotenv>=1.0.0    # Load .env for local development
